{
  "name": "Laravel (PHP 8.3 + Node)",
  "image": "mcr.microsoft.com/devcontainers/php:8.3-bullseye",
  "features": {
    "ghcr.io/devcontainers/features/node:1": { "version": "lts" }
  },
  "forwardPorts": [8000, 5173],
  "portsAttributes": {
    "8000": { "label": "Laravel" },
    "5173": { "label": "Vite" }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y sqlite3 php-sqlite3 && php -v && composer -V && node -v && npm -v"
}
